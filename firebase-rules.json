{
  "rules": {
    ".read": false,
    ".write": false,

    "sitrep": {
      ".read": "auth != null && root.child('allowedUids').child(auth.uid).val() === true",
      ".write": "auth != null && root.child('allowedUids').child(auth.uid).val() === true"
    },

    "allowedUids": {
      ".read": false,
      ".write": false
    }
  }
}
